import {makeStyles} from '@mui/styles';

export default makeStyles(() => ({
    toolbar: {
        display: 'flex',
        justifyContent: 'space-between',
    },
    loginBtn: {
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'center',
        gap: '12px',
    }
}));
